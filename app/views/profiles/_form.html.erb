<%= simple_form_for @profile do |f| %>
<%= f.input :first_name %>
<%= f.input :last_name %>
<%= f.input :phone_number %>
<%= f.input :user_id, as: :hidden, value: current_user.id %>
<%= f.submit "Valider mon profil", class: "btn btn-primary" %>
<% end %>
