<%= simple_form_for [@cheval, @review] do |f| %>
<%= f.input :rating, collection: [1,2,3,4,5] %>
<%= f.input :content %>
<center>
<%= f.submit "Ajouter une note", class: "btn btn-cheval text-center center-text" %>
</center>
<% end %>

